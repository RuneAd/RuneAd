<nav class="navbar navbar-expand-lg navbar-dark mb-0">
    <div class="container">

        <button class="btn btn-link text-white d-lg-none p-2 px-3 shadow-none"
                type="button"
                data-toggle="collapse"
                data-target="#global-nav"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
             <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="global-nav">
            <div class="navbar-collapse-header">
                <div class="row">
                    <div class="col-6 collapse-brand">
                        RuneAd
                    </div>
                    <div class="col-6 collapse-close">
                        <button type="button"
                                class="navbar-toggler"
                                data-toggle="collapse"
                                data-target="#global-nav"
                                aria-controls="navbar-primary"
                                aria-expanded="true"
                                aria-label="Toggle navigation">
                            <span></span>
                            <span></span>
                        </button>
                    </div>
                </div>
            </div>

            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('') }}">Home</a>
                </li>
                <li class="nav-item">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Premium
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('premium') }}">
                                Premium
                            </a>
                            <a class="dropdown-item" href="{{ url('sponsor') }}">
                                Sponsor
                            </a>
                            <a class="dropdown-item" href="{{ url('ads') }}">
                                Advertisements
                            </a>
                        </div>
                    </li>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('docs') }}">
                        Docs
                    </a>
                </li>
                <li class="nav-item">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Tools
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('tools/itemdb') }}">
                                OSRS Item DB
                            </a>
                            <a class="dropdown-item" href="{{ url('tools/map') }}">
                                OSRS Interactive Map
                            </a>
                        </div>
                    </li>
                </li>
                <li class="nav-item">
                    <a class="nav-link"
                        href="https://discord.gg/XH5keGQ"
                        target="_blank">
                        Join Our Discord!
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://probemas.com"
                       target="_blank">
                       Buy OSRS Gold
                    </a>
                </li>
            </ul>


            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#" rel="nofollow" id="darkmode">
                        {% if theme == "dark" %}
                        {{ icon("brightness-high", 18, 18) }}
                        {% else %}
                        {{ icon("brightness-alt-high", 18, 18) }}
                        {% endif %}
                    </a>
                </li>
                {% if user is defined %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ user.username }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ url('profile') }}">My Profile</a>

                        {% if user.isRole("owner") %}
                        <a class="dropdown-item" href="{{ url('admin') }}">Admin Panel</a>
                        {% endif %}

                        <div class="dropdown-divider"></div>

                        <a class="dropdown-item text-danger" href="{{ url('logout') }}">Sign Out</a>
                    </div>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="btn btn-primary mt-1" rel="nofollow" href="#" data-toggle="modal" data-target="#loginModal">
                        <i class="fab fa-discord mr-2 fa-lg"></i> Sign In / Register
                    </a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
