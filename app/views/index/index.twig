{% extends 'layout.twig' %}

{% block content %}
<div class="container custom-container" style="margin-top:-50px;">
    <div class="row">
        <div class="col-sm-12">
            {% include 'index/headnav.twig' %}

            <div class="content-section py-2">
                <p>{{ content|raw }}</p>
            </div>
        </div>

<div class="d-flex w-100">
    <div class="col-sm-9">
        <div class="flex-fill w-100">
            {% include 'index/listing.twig' %}
        </div>
    </div>
    <div class="col-sm-3">
        {% include 'index/listing-nav.twig' %}
    </div>
</div>
    </div>
</div>

{% endblock %}
