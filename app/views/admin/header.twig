<div class="card border-0 shadow-sm rounded-0 mb-5">
    <div class="card-body">
        <div class="d-flex align-items-center">
            <div class="flex-fill">
                <a class="text-white-50 text-uppercase small" href="{{ url('admin') }}">
                    RuneAd Admin Panel
                </a>
            </div>
            <div class="flex-fill text-right small text-white-50">
                <i class="fas fa-user-circle"></i> 
                    {{ user.username|capitalize }}
            </div>
        </div>
    </div>
</div>
